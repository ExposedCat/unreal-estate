{
	"name": "pronajemik-server",
	"version": "0.0.0",
	"scripts": {
		"dev": "bun run --watch api/index.ts",
		"typecheck": "tsc --noEmit",
		"prevercel": "mkdir -p node_modules && rm -rf node_modules/pronajemik-common && cp -a ../common ./node_modules/pronajemik-common"
	},
	"dependencies": {
		"@vercel/node": "^5.3.0",
		"@vercel/related-projects": "^1.0.0",
		"elysia": "latest",
		"pronajemik-common": "file:../common"
	},
	"devDependencies": {
		"bun-types": "latest"
	},
	"module": "src/index.js"
}
